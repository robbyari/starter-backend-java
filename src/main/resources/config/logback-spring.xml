<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration>
<configuration>
    <property name="LOG_DIR" value="logs" />
    <property name="LOG_FILE_NAME" value="master" />
    <property name="LOG_FILE_PATH" value="${LOG_DIR}/${LOG_FILE_NAME}" />
    <appender name="Console"
              class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <appender name="File-Rolling"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE_PATH}.log</file>

        <rollingPolicy
                class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>archives/logs/${LOG_FILE_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- each archived file, size max 10MB -->
            <maxFileSize>10MB</maxFileSize>
            <!-- total size of all archive files, if total size > 20GB, it will delete
                old archived file -->
            <totalSizeCap>20GB</totalSizeCap>
            <!-- 60 days to keep -->
            <maxHistory>60</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d %p %c{1} [%t] %m%n</pattern>
        </encoder>
    </appender>

    <logger name="org.springframework" level="info"
            additivity="false">
        <appender-ref ref="File-Rolling" />
    </logger>
    <logger name="com.zaxxer" level="info" additivity="false">
        <appender-ref ref="File-Rolling" />
    </logger>
    <logger name="com.pancaran" level="info" >
    	<appender-ref ref="Console" />
    </logger>
    <logger name="com.apik" level="info" >
    	<appender-ref ref="Console" />
    </logger>

    <root level="info">
        <appender-ref ref="Console"/>
        <appender-ref ref="File-Rolling" />
    </root>
</configuration>